kind: pipeline
type: docker
name: default

steps:
  - name: backend
    image: node
    commands:
      - yarn --cwd server --frozen-lockfile
      - yarn --cwd server build

  - name: frontend
    image: node
    commands:
      - yarn --cwd web --frozen-lockfile
      - yarn --cwd web build
